version: '3.7'

services:
  fake-oracle-node-stg:
    hostname: fake-oracle-node-stg
    image: fake-oracle-node-stg
    restart: always
    build:
      context: .
      args:
        - NPM_TOKEN=${NPM_TOKEN}
    logging:
      driver: "json-file"
      options:
        max-file: "5"
        max-size: "100m"
    environment:
      - ORACLE_NODE_PK=${ORACLE_NODE_PK}
      - RPC=${RPC}
      - NETWORK=rabbit.stg
      - INTERVAL=60000