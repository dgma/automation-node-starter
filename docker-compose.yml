version: "3.7"

services:
  automation-node-develop:
    hostname: automation-node-develop
    image: automation-node-develop
    restart: always
    build:
      context: .
    command: ["make", "dev"]
    volumes:
      - ./src:/app/src:cached
    env_file:
      - conf/.env
